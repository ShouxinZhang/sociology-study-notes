# Repo Agent Instructions

- 任何时候都必须将模块化，层次化思想应用起来。善于利用 tree 或者 graph 结构。主干文件绝对不能臃肿，能拆分到子模块的就到子模块里。例如书写 SKILL 时，尽可能将模板等冗长细节放到 `references` 或者 `scripts` 子文件夹里；又比如软件模块，不管是 Python、TypeScript 之类，都应该时刻注意，不要把太多功能堆放在单个文件里，而是拆分为有层次的多子文件夹结构。
- 在构建新代码之前，先审查现有代码，以减少冗余。
- 任何新变更都应尽可能简单。不要增加额外分支，也不要擅自规划未经授权的功能。
- 在任何实现中，业务成果优先。第一优先级是交付所需功能，其次是长期架构规划，最后是保持模块风格简洁高效。
- 严禁将任何新代码存放在高层级模块中（例如仓库根目录或其他高层级模块目录）。必须根据实际用途，将其放置在叶子模块目录中。
- `docs/architecture/repository-structure.md` 是仓库架构的全局入口。每次修改代码前，都必须从该文件进入并审查目标模块的索引与叶子记录，以确保变更符合仓库架构。修改完成后，立即更新目标叶子记录及其直接父索引；只有顶级模块发生变化时，才更新全局入口。
- 每个产生仓库变更、用户环境变更、数据清理或实质性故障诊断的独立任务，都必须使用 `.github/skills/dev-logs/SKILL.md`，按“一任务一日志”完成记录和索引维护；历史日志不追溯整改。
- 任何时候，都应该保持精简的生成风格, 能用5行以内描述的，绝对不要用20行以上描述; 无关的废话或者代码永远不要生成，简洁优雅，言简意赅，宁少勿多
- 作为AI，文档撰写的时候，必须要声明你的model name.

向用户解释代码时，始终从业务角度进行说明。即使用户是开发者，也要将其视为只希望了解代码能为业务做什么、代码有哪些优势与劣势的管理负责人。

执行任何代码变更之前，必须先与用户对齐方案。只有在用户确认方案可行之后，才能开始工作。【但如果用户明确说"无需确认"，则你可以自行完成】

删除文件或回滚之前，必须先在 `.agents/cache/task_name/` 中创建备份。

---

代码创作上的方法论:

- 善于高效利用CPU, GPU, 内存. 利用并行等各种方法加速
- 涉及到模型训练或者类似长时间代码任务的时候，需要设计好check point等机制，随时继续
- 创作通用性, 可复用性强的优雅代码, 在代码构筑完成后必须重新审阅一次进行精修, 多次创建的重复变量, 类, 函数等 需要重构, 冗余无用的代码需要删除.
- 注重利用重复资源, 例如.venv环境, 必须检查是否有共享环境可以使用, 防止浪费磁盘资源.
- 代码文件, 必须书写详细的注释, 没有注释的代码是灾难性的
- PLAN地图技巧, 完成长程任务的时候, 需要制作一个带有打勾框的层次地图(存放在docs/plan/task-name/plan-name.md), 按照地图持续推进打勾. 如果计划本身有问题, 可以修改计划后继续. 并且，plan遵循MVP优先，目标分解，先核心后精修，逐步`sorry free`的思想，严禁工作许久才交付，最好是用户可以阶段性获得交付结果。

---

正向提示（标准操作，强烈鼓励的行为）：

- 编写前端代码后，自行创建并运用review模块检查是否存在错误。如果有错误，持续修复，直到不再出现错误信息。
- 自动更新与变更对应的架构叶子记录及其直接父索引；顶级模块发生变化时自动更新 `repository-structure.md`。
- 能够为用户自动化完成的工作都应自动完成，而不是要求用户手动操作。（如需必要信息，应主动询问。）
- 由于你的知识很可能已经过时，在安装 SDK 或 Python 软件包服务时，请在线获取最新版本并默认安装，不要使用过时的版本号。
- 对于一些固定数据（文档的大段文字、代码函数）的迁移，请尽可能不要自行逐行生成（太慢了），而是使用 Bash，或者在 `.agent/cache/task_name/` 中使用临时 Python 代码进行迁移（计算机copy的IO速度总是非常快）。之后如需微调，再进行编辑。

负向提示（严格禁止的行为）：

- 编写代码后不进行任何检查或自主测试，从而导致大量错误和缺陷。
- 绕过 `dev-logs` Skill、把多个可独立验收的任务合并到同一日志，或完成任务后不登记日志。
- 开发新功能后不更新对应的架构叶子记录与父级索引。
- 因为懒惰而要求用户手动完成本可通过命令行或脚本自行完成的任务（反例：“等你（用户）完成 XXX 后，我（LLM）就可以做 YYY”）。严禁这种行为！如果确实无法独立完成，必须说明：在获得 XXX 信息后，才能继续进行 YYY。
- 安装过时的 SDK 或软件包版本，例如 mathlib 4.26、PyTorch 2.7 等。最新版本早已超过这些旧版本，使用它们会给用户带来版本问题。
